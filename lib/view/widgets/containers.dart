import 'package:flutter/material.dart';

Container kContainer(
    {double xHeight = 0,
    double xWidth = 0,
    Color xColor = Colors.transparent}) {
  return Container(
    height: xHeight,
    width: xWidth,
    color: xColor,
  );
}
